import React from "react";
import ReactDOM from "react-dom/client";
import HexHopApp from "./app";

ReactDOM.createRoot(document.getElementById("root") as HTMLElement).render(
  <React.StrictMode>
    <HexHopApp />
  </React.StrictMode>,
);
