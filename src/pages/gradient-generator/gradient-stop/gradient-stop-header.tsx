import "@/style/empty-checker-board.css";
import { Plus } from "lucide-react";

const GradientStopHeader = ({  }: { layerId: number }) => {
  return <> 
  <div onClick={()=>{}}>
    <Plus/>
  </div>
  </>;
};

export default GradientStopHeader;
